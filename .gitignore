# Nothing here needs npm, but just in case
node_modules/
package-lock.json
*.log

# OS junk
.DS_Store
._*
Thumbs.db
desktop.ini

# Machine-specific Claude Code settings
.claude/settings.local.json

# NOTE: docs/ is intentionally NOT ignored.
# GitHub Pages serves it straight from the repo, which is what keeps
# publishing to a plain `git push` with no CI in the loop.
